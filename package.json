{
  "name": "env-file-checker",
  "version": "1.0.5",
  "description": "Check env file against mandatory variables present in package.json file.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "bin": {
    "env-file-checker": "./index.js"
  },
  "author": "shreynik",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "https://github.com/Shreynik/env-file-checker.git"
  },
  "dependencies": {
    "chalk": "^2.4.2",
    "dotenv": "^7.0.0",
    "findup-sync": "^3.0.0"
  }
}
